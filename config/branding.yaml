# Branding configuration
# Place logo files in public/logos/ — Chainlit serves them at /public/logos/<filename>
# After changing logo_file, also update default_avatar_file_url in .chainlit/config.toml

logo_file: rogers-logo.svg
welcome_title: "5G Core Agent ready"
